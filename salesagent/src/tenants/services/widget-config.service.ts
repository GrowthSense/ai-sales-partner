// buildWidgetConfig(tenant): WidgetConfig
//   → assembles branding (color, logo, greeting), agent name, allowed domains
//   → used by the public /widget-config endpoint (fetched by the JS embed)
export class WidgetConfigService {}
